---
import SchemaOrg from "./SchemaOrg.astro";
import { buildBreadcrumbs } from "@lib/schema-org";

interface Props {
  items?: Array<{ label: string; href?: string }>;
}

const { items } = Astro.props;
---

{items && items.length > 0 && <SchemaOrg schema={buildBreadcrumbs(items)} />}
