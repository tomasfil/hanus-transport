---
import SchemaOrg from "./SchemaOrg.astro";
import { buildArticle } from "@lib/schema-org";

interface Props {
  title: string;
  description: string;
  url: string;
  publishedDate: Date;
  updatedDate?: Date;
}

const props = Astro.props;
---

<SchemaOrg schema={buildArticle(props)} />
